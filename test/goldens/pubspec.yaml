name: goldens
description: Goldens for amplify_codegen
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  amplify_datastore_plugin_interface: ^0.3.0-0
  flutter:
    sdk: flutter

dev_dependencies:
  amplify_lints: ^1.0.0
  lints: ^1.0.0
